import { DayContent } from '../../types';

export const w5d6Data: DayContent = {
  day: 6,
  title: "RAG 进阶：Agent RAG 与 GraphRAG",
  shards: 1,
  steps: [
    { type: 'theory', content: "🚀 **今日主题：RAG 的进化方向**\n\n基础 RAG 有局限——只能做简单的「一问一检索一回答」。进阶 RAG 能处理更复杂的场景：多跳推理、知识图谱、多知识库路由。" },
    { type: 'video', url: "", content: "视频：从基础 RAG 到 Agent RAG —— RAG 的下一代进化" },
    { type: 'theory', content: "🤖 **第一章：Agent RAG**\n\n基础 RAG 是固定流程：检索 → 生成。\n\nAgent RAG 让 AI 自主决定：\n\n```\n1. 判断是否需要检索（简单问题直接回答）\n2. 决定检索什么（可能需要拆分子问题）\n3. 评估检索结果（不够好就换个方式再检索）\n4. 综合多次检索结果生成回答\n```\n\n像一个有判断力的研究员，而不是机械的搜索引擎。" },
    { type: 'theory', content: "🕸️ **第二章：GraphRAG（知识图谱 RAG）**\n\n传统 RAG 把文档当成独立的块。GraphRAG 用**知识图谱**来表示知识之间的关系：\n\n```\n传统 RAG：[块1] [块2] [块3] （独立的片段）\n\nGraphRAG：\n  [张三] --任职于--> [ABC 公司]\n  [ABC 公司] --位于--> [北京]\n  [张三] --负责--> [AI 项目]\n```\n\n优势：能回答需要**多跳推理**的问题（「张三所在公司在哪个城市？」）" },
    { type: 'theory', content: "🔀 **第三章：多知识库路由**\n\n企业通常有多个知识库：\n\n```\n用户问题 → 路由器（Router）→\n  ├── 人事相关 → HR 知识库\n  ├── 技术相关 → 技术文档库  \n  ├── 财务相关 → 财务知识库\n  └── 通用问题 → 直接用 LLM 回答\n```\n\n路由器是一个分类器，判断问题应该去哪个知识库检索。" },
    { type: 'theory', content: "🔄 **第四章：对话式 RAG**\n\n支持多轮对话的 RAG 需要处理：\n\n| 挑战 | 解决方案 |\n|------|----------|\n| **指代消解** | 「它」指什么？→ 用对话历史推断 |\n| **上下文积累** | 基于前面的对话继续提问 |\n| **历史管理** | 对话太长怎么办？→ 摘要压缩 |\n| **搜索演化** | 根据对话进展动态调整检索策略 |" },
    { type: 'theory', content: "🔧 **实战时间！**\n\n**🎯 实战任务：设计一个进阶 RAG 系统**\n\n场景：为一家有 3 个部门（HR、技术、财务）的公司设计 RAG 系统。\n\n需要考虑：\n1. 路由：怎么判断问题该去哪个知识库？\n2. 多轮对话：怎么处理追问？\n3. 权限：不同员工能查不同知识库" },
    { type: 'quiz', question: "1. 【区别】Agent RAG 和基础 RAG 的最大区别是？", options: ["速度更快", "AI 可以自主判断是否需要检索、检索什么、结果够不够好——像一个会思考的研究员", "使用不同的模型"], correct: 1 },
    { type: 'quiz', question: "2. 【场景】用户问「比较 A 产品和 B 产品的优缺点」，Agent RAG 会怎么做？", options: ["一次检索搞定", "分别检索 A 产品和 B 产品的文档，再综合对比——自动拆分成两个子检索任务", "回答不了"], correct: 1 },
    { type: 'fill', question: "3. 【概念】AI 自主判断下一步行动的能力叫做 ___ 能力。", parts: ["自主决策的能力叫做", "___", "能力。"], options: ["Agent（智能体）", "搜索", "计算"], correct: "Agent（智能体）" },
    { type: 'quiz', question: "4. 【概念】GraphRAG 中的「Graph」指什么？", options: ["图片", "知识图谱——用节点和关系边来表示知识之间的连接", "图表"], correct: 1 },
    { type: 'quiz', question: "5. 【优势】传统 RAG 很难回答但 GraphRAG 能回答的问题类型是？", options: ["简单事实查询", "多跳推理——需要跨多个实体追踪关系的问题", "所有问题"], correct: 1 },
    { type: 'match', question: "6. 【连线】RAG 技术对比", pairs: [{ left: "基础 RAG", right: "检索→生成，固定流程" }, { left: "Agent RAG", right: "AI 自主判断检索策略" }, { left: "GraphRAG", right: "基于知识图谱的多跳检索" }, { left: "Multi-query RAG", right: "一个问题拆成多角度检索" }] },
    { type: 'practice', task: "【直接写 Prompt】围绕「张三任职年限和项目经历查询」，写一个可以直接复制给大模型使用的 Agent RAG 与 GraphRAG Prompt。要求包含：角色/目标、输入信息、执行步骤、输出格式、边界限制。", rubric: "是否能直接复制使用；是否围绕 Agent RAG 与 GraphRAG 和「张三任职年限和项目经历查询」展开；是否包含角色/目标、输入、步骤、输出格式和边界；是否能降低「代词消解错误或检索不完整」风险；语言是否清晰不绕。", placeholder: "你是...\n你的目标是...\n用户会提供...\n请按以下步骤完成：\n1. ...\n2. ...\n输出格式：...\n边界限制：...", minLength: 80 },
    { type: 'practice', task: "【结构化表达】为「张三任职年限和项目经历查询」写一份可直接复制使用的任务说明。必须面向「HR 知识库用户」，并明确输入、处理步骤、输出格式和禁止事项。", rubric: "结构是否清晰；输入/步骤/输出是否完整；是否符合 HR 知识库用户 的理解水平；禁止事项是否能降低「代词消解错误或检索不完整」风险。", placeholder: "## 输入\n...\n## 处理步骤\n1. ...\n## 输出格式\n...\n## 禁止事项\n...", minLength: 80 },
    { type: 'practice', task: "【边界处理】列出「张三任职年限和项目经历查询」在真实使用中最容易失败的 4 种情况，并分别写出 AI 应该如何回应或兜底。", rubric: "是否包含真实边界场景；每个兜底是否具体可执行；是否避免让 AI 编造；是否覆盖「代词消解错误或检索不完整」这个核心风险。", placeholder: "失败场景 1：...\nAI 兜底：...\n失败场景 2：...\nAI 兜底：...", minLength: 80 },
    { type: 'practice', task: "【输出格式设计】为「Agent RAG ReAct 流程」设计一个标准输出模板。要求模板字段清楚、顺序合理，并说明每个字段为什么需要。", rubric: "模板是否能支撑 3 轮 Thought/Action/Observation + 综合答案；字段是否完整且不冗余；是否说明字段用途；是否便于后续复制、评审或程序解析。", placeholder: "## 输出模板\n字段 1：...\n字段 2：...\n\n## 字段说明\n- 字段 1 用来...", minLength: 80 },
    { type: 'practice', task: "【质量评审】假设另一个同学提交了「Agent RAG ReAct 流程」。请写一份评分标准，至少包含 5 个维度，并给出每个维度的满分表现和扣分点。", rubric: "是否有 5 个以上评分维度；维度是否贴合 Agent RAG 与 GraphRAG；是否区分满分表现和扣分点；是否能指导同学改进。", placeholder: "1. 维度：...\n满分表现：...\n扣分点：...\n2. 维度：...", minLength: 80 },
    { type: 'practice', task: "【示例驱动】为「张三任职年限和项目经历查询」补充 2 组高质量示例：一组是简单输入，一组是困难输入。每组都要写出理想输出，并解释困难输入难在哪里。", rubric: "是否有简单和困难两组示例；输入输出是否配套；困难点解释是否准确；示例是否能帮助大模型学会 Agent RAG 与 GraphRAG。", placeholder: "示例 A（简单）：\n输入：...\n理想输出：...\n\n示例 B（困难）：\n输入：...\n理想输出：...\n困难点：...", minLength: 80 },
    { type: 'practice', task: "【约束优化】把「Agent RAG ReAct 流程」压缩成一个更稳的版本：要求减少废话，但不能丢掉角色、任务、边界、输出格式和检查标准。", rubric: "是否保留关键约束；是否去掉空话；是否更容易执行；是否仍能防止「代词消解错误或检索不完整」。", placeholder: "精简版：...\n保留的关键约束：...\n删掉的废话：...\n风险控制：...", minLength: 80 },
    { type: 'practice', task: "【真实迁移】把今天的「Agent RAG 与 GraphRAG」迁移到你自己的一个工作/学习场景。请写出场景、目标用户、任务输入、期望输出和一版初始方案。", rubric: "场景是否真实具体；是否能体现 Agent RAG 与 GraphRAG；输入输出是否明确；方案是否可执行；是否说明如何评估好坏。", placeholder: "我的场景：...\n目标用户：...\n输入：...\n输出：...\n初始方案：...\n评估方式：...", minLength: 80 },
    { type: 'practice', task: "【综合大题】完整产出一份「Agent RAG ReAct 流程」，用于「张三任职年限和项目经历查询」。必须包含：1）背景和目标；2）面向对象：HR 知识库用户；3）完整方案正文；4）输出格式或交付物；5）至少 3 个质量检查点；6）针对「代词消解错误或检索不完整」的风险预案。要求写到可以直接拿去给 AI 或团队使用。", rubric: "是否完整覆盖 6 个要求；是否紧扣 Agent RAG 与 GraphRAG 和 张三任职年限和项目经历查询；交付物是否具体、可执行、可复制；是否有明确质量检查；是否认真处理核心风险「代词消解错误或检索不完整」；是否避免空话和泛泛而谈。", placeholder: "## 1. 背景和目标\n...\n## 2. 面向对象\n...\n## 3. 完整方案\n...\n## 4. 输出格式/交付物\n...\n## 5. 质量检查点\n- ...\n## 6. 风险预案\n...", minLength: 220 },
    { type: 'theory', content: "🎉 **Day 6 完成！**\n\n你已经掌握了 RAG 的前沿进阶技术。\n\n**Day 7 预告**：Week 5 Boss Battle —— **知识龙的考验！** RAG 全流程综合大挑战。" }
  ]
};
